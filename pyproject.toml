[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
skinpy = "^0.0.4"
mediapipe = "^0.10.18"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
